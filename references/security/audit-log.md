# Bitácora de auditorías de seguridad

Auditoría continua de Arcade Vault: Supabase (RLS, funciones, advisors), app Next.js (auth, actions, headers), secretos/entorno y dependencias. Mantenida por el agente `security-auditor` — solo lectura sobre el proyecto, ver `.claude/agents/security-auditor.md`.

Severidades: crítico (explotable hoy, impacto alto) · alto · medio · bajo · informativo (buena práctica, no vulnerabilidad).

## Hallazgos abiertos

| ID     | Severidad   | Superficie | Ubicación                                        | Hallazgo                                                                                                                                                                                                                                                                                     | Estado             | Detectado  |
| ------ | ----------- | ---------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ---------- |
| SEC-01 | alto        | app        | `app/api/contact/route.ts:12`                    | Endpoint público sin rate limiting, captcha ni límite de tamaño: cualquiera puede enviar correos ilimitados vía la `RESEND_API_KEY` del proyecto (spam, coste, baneo).                                                                                                                       | abierto            | 2026-08-18 |
| SEC-02 | medio       | app        | `app/api/contact/route.ts:37`                    | `name`/`email`/`message` se interpolan sin escapar en el `html` del correo → inyección de HTML/enlaces de phishing en el buzón del destinatario.                                                                                                                                             | abierto            | 2026-08-18 |
| SEC-03 | medio       | db/config  | Supabase Auth (dashboard)                        | `auth_leaked_password_protection` sigue DESHABILITADA pese a que spec 16 la declara aplicada — paso manual no aplicado o revertido.                                                                                                                                                          | abierto            | 2026-08-18 |
| SEC-04 | medio       | app        | `lib/auth/actions.ts:11-16`                      | `getOrigin()` arma `emailRedirectTo`/`redirectTo` desde `x-forwarded-host`/`host` sin allow-list → host-header injection en links de confirmación/OAuth.                                                                                                                                     | resuelto (spec 17) | 2026-08-18 |
| SEC-05 | medio       | deps       | `next@16.2.12` → `sharp <0.35.0`, `postcss`      | CVEs high heredadas en dependencias de producción de Next (libvips CVE-2026-33327/33328/35590/35591; postcss XSS y path traversal por `sourceMappingURL`).                                                                                                                                   | abierto            | 2026-08-18 |
| SEC-06 | medio       | app        | `app/auth/callback/route.ts:14`, `confirm:21,36` | Open redirect confirmado: `${origin}${next}` con `next=@evil.com/` produce `https://sitio@evil.com/` (userinfo), host final atacante. Phishing post-login creíble; `type` además se castea a `EmailOtpType` sin allow-list.                                                                  | resuelto (spec 17) | 2026-08-18 |
| SEC-07 | bajo        | app        | `app/api/contact/route.ts:13`                    | `await request.json()` sin `try/catch` (500 no manejado) y campos sin límite de longitud → payloads arbitrariamente grandes.                                                                                                                                                                 | abierto            | 2026-08-18 |
| SEC-08 | bajo        | app        | `lib/actions/scores.ts:11-17`                    | `getTopScoresAction` acepta `limit` arbitrario desde el cliente (server action pública) sin cota máxima.                                                                                                                                                                                     | abierto            | 2026-08-18 |
| SEC-09 | bajo        | app        | `lib/auth/actions.ts:18,75`                      | `signUpAction`/`updateNicknameAction` no validan en servidor longitud/charset de `nickname` ni formato de email/password.                                                                                                                                                                    | resuelto (spec 17) | 2026-08-18 |
| SEC-10 | bajo        | deps       | `nanoid <3.3.18`                                 | CVE high (loop infinito con `size` 0) en dependencia transitiva de producción.                                                                                                                                                                                                               | abierto            | 2026-08-18 |
| SEC-11 | informativo | app        | `lib/auth/actions.ts:35,49,94`                   | `error.message` de Supabase se devuelve crudo al cliente → filtra detalles internos y facilita enumeración de cuentas.                                                                                                                                                                       | resuelto (spec 17) | 2026-08-18 |
| SEC-12 | informativo | db/config  | Supabase Auth (dashboard)                        | Min password length (8) y rate limit de signup por IP de spec 16 no son verificables por SQL ni por advisor — requieren verificación manual en el dashboard.                                                                                                                                 | abierto            | 2026-08-18 |
| SEC-13 | informativo | db         | políticas de `profiles` y `scores`               | `auth_rls_initplan`: las 3 políticas usan `auth.uid()` sin envolver en `(select ...)` → re-evaluación por fila. Es rendimiento, no vulnerabilidad.                                                                                                                                           | abierto            | 2026-08-18 |
| SEC-14 | medio       | app        | `lib/auth/actions.ts:39-51,18-37`                | Login/signup pasan por server actions con el cliente de servidor: Supabase Auth ve siempre la IP del servidor, no la del atacante → el rate limit por IP (spec 16) colapsa en un único bucket; sin freno propio, habilita credential stuffing y bloquea a usuarios legítimos por saturación. | resuelto (spec 17) | 2026-08-19 |
| SEC-15 | bajo        | app        | `proxy.ts:33-37`                                 | En la rama de redirect a `/auth` se devuelve un `NextResponse.redirect` nuevo sin copiar las cookies refrescadas por `getAll/setAll`: si el token rotó en esa petición, el refresh token nuevo se descarta y la sesión puede caerse.                                                         | resuelto (spec 17) | 2026-08-19 |
| SEC-16 | bajo        | app        | `app/auth/page.tsx:8-9`                          | El parámetro `error` de la URL se refleja tal cual en la pantalla de login (texto escapado por React, sin XSS) → un enlace `/auth?error=...` permite montar mensajes falsos creíbles en el propio dominio (phishing de credenciales).                                                        | abierto            | 2026-08-19 |
| SEC-17 | informativo | app        | `lib/supabase/queries.ts:133-141`                | `getMyBestScores` lee con el cliente anon sin cookies: la restricción por usuario es solo el `.eq("user_id", …)` de la app, no RLS. Hoy sin IDOR (el id viene siempre de `getCurrentUser`), pero un call-site futuro con id de la request lo vuelve IDOR directo.                            | abierto            | 2026-08-19 |

## Riesgos aceptados (documentados en specs)

| ID     | Superficie | Hallazgo                                                                        | Spec que lo acepta                                                            |
| ------ | ---------- | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| ACC-01 | app        | Content-Security-Policy, HSTS y Permissions-Policy ausentes en `next.config.ts` | spec 16 — "Fuera de alcance (diferido)"                                       |
| ACC-02 | db         | `rls_auto_enable()` ejecutable por `anon`/`authenticated` (SECURITY DEFINER)    | spec 15 — infraestructura de plataforma, fuera de alcance                     |
| ACC-03 | db         | `SELECT` público con `USING (true)` en `scores` y `games`                       | spec 06 — lectura pública deliberada del catálogo/leaderboard                 |
| ACC-04 | db         | Filas históricas de `scores` con `user_id` null (sin backfill)                  | specs 14 y 15 — marcas anónimas históricas conservadas                        |
| ACC-05 | db         | Sin políticas de `UPDATE`/`DELETE` en `scores` (denegado por defecto)           | spec 15 — comportamiento deseado                                              |
| ACC-06 | deps       | `next@16.2.12` pineado y desactualizado frente a `16.3.1`                       | `CLAUDE.md`/`AGENTS.md` — pin deliberado (ver SEC-05 para las CVEs concretas) |
| ACC-07 | app        | Sin recuperación de contraseña, cambio de email ni borrado de cuenta            | spec 13 — "Fuera de alcance (diferido)"                                       |
| ACC-08 | app        | `/games/[id]/jugar` jugable sin login (juego anónimo permitido)                 | spec 13 — decisión de producto explícita                                      |

## Corridas

### 2026-08-20 (2) — fixes post-PR de spec 17

- Dos reviews automáticos de seguridad sobre el commit/push de spec 17 detectaron: fail-open en `checkRateLimit` (error de DB → `allowed: true`), y una condición de carrera (TOCTOU) entre `checkRateLimit`/`recordAttempt` — requests concurrentes podían superar `MAX_ATTEMPTS`.
- Fix: se reemplazaron ambas funciones por `consumeRateLimit(ip, action)`, que llama a una función Postgres nueva (`auth_rate_limit_attempt`, `security definer`, `pg_advisory_xact_lock` por `ip+action`) que hace el conteo y el insert de forma atómica en una sola transacción. Fail-closed: si el RPC falla, bloquea el intento en vez de dejarlo pasar.
- El primer intento de migración solo revocó `EXECUTE` de `public` en la función nueva; `has_function_privilege` mostró que `anon`/`authenticated` seguían con `EXECUTE` (mismo patrón que `handle_new_user`/`rls_auto_enable`, ACC-02 — Supabase otorga el grant explícito a esos roles, no vía `PUBLIC`). Corregido con un segundo `revoke ... from anon, authenticated`; confirmado `has_function_privilege` → `false`/`false`.
- Verificado: 10 llamadas concurrentes a `consumeRateLimit` con la misma IP → exactamente 5 `allowed: true`, sin excedente.
- El cuarto hallazgo del review (IP de `x-forwarded-for` sin validar contra proxy confiable) ya estaba documentado como riesgo aceptado en `specs/17-hardening-auth.md` — no se tocó.

### 2026-08-20 — cierre de spec 17 (hardening de auth)

- Implementación de `specs/17-hardening-auth.md`: rate limit propio de login/signup (`auth_rate_limits`, tabla nueva con RLS sin políticas + cliente admin service-role), origen confiable vía `NEXT_PUBLIC_SITE_URL`, `safeNext()` + allow-list de `EmailOtpType` en los callbacks de auth, mensajes de error genéricos (`mapAuthError`), validación server-side de `nickname`, y fix de cookies descartadas en `proxy.ts`.
- Resueltos: 6 (SEC-04, SEC-06, SEC-09, SEC-11, SEC-14, SEC-15) / Nuevos: 0 / Sin cambio: el resto.
- Desvío del spec original documentado en `specs/17-hardening-auth.md`: el cliente de servidor (`lib/supabase/server.ts`) usa la anon key y no puede escribir en una tabla con RLS sin políticas; se agregó `SUPABASE_SERVICE_ROLE_KEY` + `lib/supabase/admin.ts`, uso exclusivo de `lib/auth/rate-limit.ts`.
- Pendiente real: `NEXT_PUBLIC_SITE_URL` sigue vacía en `.env` (no hay URL de producción todavía) — SEC-04 está resuelto a nivel de código (la env var tiene prioridad si está seteada), pero el fallback por headers sigue siendo el camino activo hasta que se configure.

### 2026-08-19 — alcance: app (componentes de autenticación)

- Revisión dirigida a auth: `app/auth/**`, `lib/auth/*`, `proxy.ts`, `lib/session.ts`, `lib/actions/scores.ts`, `lib/supabase/*`, más revalidación de RLS/`auth.uid()` y advisors. Foco en auth bypass e IDOR.
- Nuevos: 4 (SEC-14 rate limit por IP colapsado en la IP del servidor, SEC-15 cookies perdidas en el redirect del proxy, SEC-16 `error` reflejado en `/auth`, SEC-17 lectura de "mis marcas" sin RLS) / Resueltos: 0 / Sin cambio: SEC-01..SEC-13 (SEC-06 reclasificado de bajo a medio: open redirect confirmado por userinfo `@`).
- Sin auth bypass ni IDOR explotables: `getCurrentUser`/`getCurrentProfile` usan `auth.getUser()` (cero `getSession()` en el repo), `/perfil` está protegido en proxy y en la propia page, `saveScoreAction` exige sesión y toma `user_id`/`nickname` del servidor (nunca del cliente), y `getTopScores` no expone `user_id` al cliente (solo `isMine`).
- Revalidación DB sin regresiones: `profiles` RLS on con select/update `auth.uid() = id`; `scores` INSERT `WITH CHECK (auth.uid() = user_id)`; `scores_best` `security_invoker=true`; `handle_new_user` con `search_path=""` y sin EXECUTE para anon/authenticated/public. Advisors: solo `rls_auto_enable` (ACC-02) y leaked password protection (SEC-03, sigue deshabilitada).
- `lib/session.ts` es solo un historial local (`localStorage`) sin relación con Supabase Auth ni con el leaderboard real: no aporta superficie de auth.

### 2026-08-18 — alcance: all

- Primera corrida post-merge de specs 15 y 16: el baseline de base de datos de spec 15 está íntegramente confirmado en la DB real; el hueco restante es la config manual de Auth y la superficie de aplicación (`/api/contact` sobre todo).
- Nuevos: 13 (SEC-01 a SEC-13) / Resueltos: 5 hallazgos del `checklist.md` heredado (RLS en `profiles`, INSERT restringido en `scores`, `scores_best` con `security_invoker`, `EXECUTE` revocado en `handle_new_user`, 3 headers de seguridad en `next.config.ts`) / Sin cambio: 0.
- Revalidación de spec 15 en la DB real: `profiles.relrowsecurity=true` con políticas select/update `auth.uid() = id` y sin INSERT explícito; `scores` INSERT con `WITH CHECK (auth.uid() = user_id)`; `scores_best` con `reloptions = {security_invoker=true}`; `handle_new_user` con `search_path=""` y `has_function_privilege` false para `anon`/`authenticated`/`public`. Sin regresiones.
- Secretos: `.gitignore` cubre `.env*` y `.mcp.json`; `git ls-files` solo lista `.env.template` (sin valores); ningún uso de `service_role` en el repo; solo `RESEND_API_KEY` y `SUPABASE_DB_PASSWORD` son server-only y ninguno se lee desde componentes `"use client"`.
